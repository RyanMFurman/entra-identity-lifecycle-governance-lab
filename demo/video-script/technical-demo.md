# Three-to-Five-Minute Technical Demo

Show `data/hr.json`, `config/policy.json`, then run setup, failure injection, normal validation (exit 2), investigation, remediation, validation, tests, and teardown. Explain that policy is independent from SQLite and the future Graph adapter. Do not edit out the intentional failure.
