# Demo

Evidence: `IMPLEMENTED LOCALLY`; investigation evidence: `SIMULATED INVESTIGATION`.

From the repository root:

```powershell
pwsh ./demo/start-demo.ps1
```

The launcher initializes deterministic state, transfers Jasmine Reed to Engineering while retaining a Finance entitlement, detects stale access and a toxic combination, generates investigation evidence, remediates access, verifies compliance, and produces reports under `generated/reports/`.

Exit codes: `0` success, `1` expected failure missing, `2` validation findings, `3` demo failure injection error, `4` post-remediation validation error.
